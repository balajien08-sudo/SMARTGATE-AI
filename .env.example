# Database Connection (Optional: if omitted, automatic in-memory store is used)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smartgate_ai

# JWT Authentication Secret
JWT_SECRET=smartgate_ai_secure_jwt_secret_key_2026

# Server Port
PORT=5001

# Client URL (for CORS)
CLIENT_URL=http://localhost:5173

# Environment
NODE_ENV=development
